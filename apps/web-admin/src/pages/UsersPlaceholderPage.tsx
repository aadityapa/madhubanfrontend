export function UsersPlaceholderPage() {
  return (
    <div>
      <h1>Users</h1>
      <p style={{ color: "var(--color-text-muted)" }}>
        Connect <code>getUsers()</code> from <code>@madhuban/api</code> and add
        tables when you implement admin user management.
      </p>
    </div>
  );
}
